import React from 'react';
import logo from './logo.png?inline';

const Bruno = ({ width }) => {
  return <img src={logo} width={width} alt="Brainal" style={{ borderRadius: '20%' }} />;
};

export default Bruno;
